An extremely primitive terminal multiplexer that tries to emulate screen and tmux functionality written in bash. Made as a project for Operating Systems classes.
Use
```
multiplexer.sh -h
```
for more help
